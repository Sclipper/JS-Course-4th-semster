extends layout

block content
    h1 Add a new joke
    form(name="input", action="/storejoke", method="post")
        input(type="text", name="joke", placeholder="Add Joke")
        br
        br
        input(type="submit", value="Submit")
        br
        br
       a(href="/home") Go Back

       h4=optionalMessage